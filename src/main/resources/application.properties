spring.application.name=metrics-aggregator

spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5444/mydb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}


# Hibernate properties
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public


# Включаем выброс исключения, когда обработчик запроса не найден
# spring.mvc.throw-exception-if-no-handler-found=true
# Отключаем обработку статических ресурсов Spring (рекомендуется для корректной работы)
# spring.web.resources.add-mappings=false